package dominios;

public interface AcessosUsuario {
    public boolean auth = false;
    // username;
    // password;
    // roles

    // meth => password === password || obj boolean, id, role; => token

}
